# Leave unset to deploy the fully functional local/demo game mode.
# For live authentication and cross-device sync, use the public HTTPS URL of
# your separately deployed backend — no trailing slash.
VITE_API_URL=https://your-api.example.com
